# There are two ways of building tree one is recursive and the other one is using the index concept in the array.
class Tree:
    def __init__(self,val=None) -> None:
        self.val = val
        self.left = None
        self.right = None
    def buildTree(self):
        d = int(input("Enter the node value"))
        node = Tree(d)
        l = int(input("Enter the left value of the node"))
        bu

tree = Tree()





